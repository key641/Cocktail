# AI 酒保基础重构计划

## 结论

不进行大爆炸式重写。保留产品定位、鸡尾酒知识库、SVG 视觉、现有领域逻辑和测试，通过分阶段迁移重建前端底座并收敛 Agent 主路径。

重构期间冻结非必要新功能。每完成一个阶段，都必须保持核心用户流程可运行并可回退。

## 保留范围

- 私人 AI 酒保的产品定位与中文语气
- 经典酒单、配方、材料和口味知识
- 鸡尾酒与酒保 SVG 视觉资产
- 探索、材料匹配、酒单详情、跟做、收藏和反馈能力
- 领域层、安全规则和现有自动化测试

## 需要重建的底座

- 将 `App` 收敛为页面组合与全局入口，不再承载完整业务流程
- 将聊天会话、推荐结果、跟做流程和酒柜分别管理状态
- 将现有三份全局 CSS 收敛为设计变量、基础样式、布局和组件样式
- 让后端成为推荐结果的唯一来源，移除前端第二套推荐逻辑
- 明确区分本地、预览和线上 API 配置
- 为五条核心手机端流程建立端到端验证

## 阶段 0：建立安全基线

### 工作项

1. 提交当前分支并标记可回退基线。
2. 清理临时日志和测试产物，不混入正式改动。
3. 固定当前酒单、用户数据结构和 Agent 响应结构。
4. 记录五条核心流程的当前行为与截图。

### 验收

- 当前构建和测试通过。
- 任意重构提交都可以单独回退。
- 新功能进入冻结状态。

## 阶段 1：重建前端底座

### 工作项

1. 提取页面导航与滚动恢复逻辑。
2. 建立独立的聊天会话状态模块。
3. 建立独立的推荐结果与跟做流程状态模块。
4. 建立统一设计变量，包括颜色、字号、间距、圆角、阴影和动效。
5. 按聊天页、详情页、跟做页、酒柜页、首页和酒单页的顺序迁移样式。
6. 每迁移一个页面，删除对应旧样式，不保留跨文件覆盖。

### 验收

- `App` 只组合页面与协调少量全局状态。
- 任一组件只有一个正式样式来源。
- 375px 至 768px 宽度下没有裁切、重叠或不可操作区域。
- 输入框在软键盘弹出前后保持稳定。

## 阶段 2：收敛 Agent 主路径

### 工作项

1. 将固定管线设为唯一正式执行路径。
2. 将直接酒名搜索优先路由到确定性搜索工具。
3. 分离意图解析、候选检索、排序、文案生成和安全检查。
4. 将 ReAct 保留为实验开关，不参与默认降级链路。
5. 移除前端本地推荐答案，只保留明确的连接失败提示。
6. 为每轮请求记录解析、检索、排序、模型和首字延迟。

### 验收

- 同一请求只有一个推荐真相来源。
- 第一条思考状态在提交后立即出现。
- 第二轮对话稳定继承会话状态。
- 模型失败和业务无结果有不同的用户提示。

## 阶段 3：稳定交付

### 核心自动化流程

1. 首页进入聊天并完成两轮对话。
2. 酒单进入详情并开始跟做。
3. 完成全部步骤并上传成品照片。
4. 管理酒柜材料并查看可制作酒款。
5. 对模型回答点赞、点踩并提交中文反馈。

### 适配视口

- 375 × 812
- 390 × 844
- 430 × 932
- 768 × 1024
- 桌面宽屏

### 验收

- 核心流程端到端测试通过。
- 关键页面视觉快照通过。
- 本地开发默认连接本地后端。
- 线上环境变量缺失时给出清晰诊断。
- 弱网、超时、空状态和失败状态均可恢复。

## 实施顺序

第一批只处理聊天页及其底座，因为它同时暴露状态管理、流式响应、输入框、推荐卡片和 Agent 路径问题。聊天链路稳定后，再迁移详情与跟做，随后处理酒柜、首页和酒单。

任何阶段都不允许通过新增全局覆盖样式或复制推荐逻辑来绕过结构问题。
