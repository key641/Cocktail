# Design System

## Direction

产品界面是一张会回应用户的私人酒保工作台。它借用酒谱、服务托盘与调酒工作台的秩序感，但不复刻传统酒吧装潢，也不把页面包成手机样机。

## Visual Language

- 雾白工作台是主背景，墨绿承担主要行动和选中状态，浅酒液绿只用于酒保杯与柔和提示。
- 大面积留白、细分隔线和少量柔和投影建立层级；不使用厚重卡片墙、霓虹、渐变文字或装饰性玻璃拟态。
- 酒保杯是核心生命体，尺寸可以显著大于普通内容图标，表情和液体动效负责传递状态。
- 桌面采用非对称双栏工作台，移动端回到单列服务流程。

## Typography

- 中文展示标题使用宋体气质的系统字体栈，正文与控制使用现代无衬线系统字体。
- 标题紧凑、正文舒展；首屏标题允许形成有节奏的自然换行。
- 操作文案直接描述用户要做的事，避免技术术语和泛化按钮名。

## Components

- 主行动使用整块墨绿服务托盘，次行动保持白底细线与轻量图标。
- 卡片圆角以 13–18px 为主，浮层和面板可使用 20px；胶囊仅用于工具按钮和短状态。
- 图标统一为 `currentColor` 细线 SVG，不使用 emoji 或混合图标家族。
- 底部导航在桌面和移动端都是悬浮工具托盘，选中项使用墨绿实底。
- 聊天输入区保持稳定高度，文本区最大高度受控，不随移动端键盘无限放大。
- 触控设备上的按钮与图标操作区不小于 44×44px；选中、禁用、加载、悬停、按压与键盘焦点状态必须保持同一反馈语言。

## Motion

- 动效集中在酒保杯的呼吸、表情和液体状态，以及主要行动的轻微位移反馈。
- 过渡使用快速、自然的 ease-out，不延迟内容出现。
- 所有自定义动画都服从 `prefers-reduced-motion`。

## Responsive Rules

- `840px` 以上使用完整工作台双栏；以下切换为单列移动界面。
- `641–840px` 的竖屏平板使用紧凑双栏工作台，避免把手机单列简单拉长。
- 低高度手机横屏使用左右分区，保证主行动在首屏可见，底部工具托盘移到右侧。
- 移动端使用 `100dvh`，为安全区保留底部导航和输入区间距。
- 酒单在桌面为三列错落目录，移动端为稳定两列；SVG 原子画廊为桌面四列、移动端两列。
